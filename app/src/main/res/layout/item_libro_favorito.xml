<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/card_favorito"
        android:layout_width="0dp"
        android:layout_height="70dp"
        android:layout_marginEnd="15dp"
        app:cardBackgroundColor="#D3D3D3"
        app:cardCornerRadius="4dp"
        app:cardElevation="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/btn_corazon"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/txt_titulo_fav"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="15dp"
            android:text="" />
    </androidx.cardview.widget.CardView>

    <ImageView
        android:id="@+id/btn_corazon"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:src="@android:drawable/btn_star_big_on"
        android:contentDescription="Favorito"
        app:tint="#000000"
        app:layout_constraintBottom_toBottomOf="@id/card_favorito"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/card_favorito" />

</androidx.constraintlayout.widget.ConstraintLayout>